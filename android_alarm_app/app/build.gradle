plugins {
    id "com.android.application"
}

android {
    namespace "com.example.oneshotalarm"
    compileSdk {
        version = release(36) {
            it.minorApiLevel = 1
        }
    }

    defaultConfig {
        applicationId "com.example.oneshotalarm"
        minSdk 23
        targetSdk 36
        versionCode 1
        versionCode 2
        versionName "1.1"
    }
}

dependencies {
    implementation "com.google.android.gms:play-services-wearable:19.0.0"
}
